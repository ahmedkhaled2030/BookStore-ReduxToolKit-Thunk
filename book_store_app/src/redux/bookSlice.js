import { createSlice } from '@reduxjs/toolkit';


const bookSlice = createSlice({
    name: 'book',
    initState: {books : null},
    reducers: {
        
    }
})

export default bookSlice.reducer; 
